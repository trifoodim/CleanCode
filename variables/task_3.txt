7.1

flag - has_number
// индикатор наличия в ячейке массива не нулевого значения

cols - column_locked
// индикатор того, что столбец матрицы уже использован при вычислении определителя

first - drop_first
// исключить первый член при вычислении суммы


7.2

done = False
while (type(node) is not DummyNode) and not done:
    ...
// done - флаг завершения цикла

found = [False] * hash_table.size
for h in hashes:
    found[h] = True
// found - признак того, что ячейка в хэш-таблице занята


7.3

for row in range(mx.shape[0]):
    for col in range(mx.shape[1]):
        ...
// строка и столбец матрицы


7.4

fuel_min, fuel_max
// границы диапазона топлива для расчётов

input_file, output_file
// имена входного и выходного файлов

pos = k2 >= 0
neg = k2 < 0
// массивы флагов, отмечающих положительные и отрицательные элементы


7.5

n - size
// размер квадратной матрицы

left - left_part
// левая часть системы линейных уравнений

invY - inv_right_matrix
// инвертированная матрица из правой части системы линейных уравнений